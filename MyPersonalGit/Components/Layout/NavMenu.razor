@using MyPersonalGit.Data
@using MyPersonalGit.Services
@using Microsoft.Extensions.Localization
@using MyPersonalGit.Resources
@inject INotificationService NotificationService
@inject CurrentUserService UserService
@inject IStringLocalizer<SharedResource> L

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">MyPersonalGit</a>
        <div class="ms-auto d-flex gap-2">
            @if (UserService.IsLoggedIn)
            {
                <a href="/notifications" class="btn btn-sm btn-outline-light position-relative">
                    <i class="bi bi-bell"></i>
                    @if (UnreadCount > 0)
                    {
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            @UnreadCount
                        </span>
                    }
                </a>
                <a href="/profile/@UserService.Username" class="btn btn-sm btn-outline-light">
                    <i class="bi bi-person-circle"></i> @L["Nav_Profile"]
                </a>
                <a href="/settings" class="btn btn-sm btn-outline-light">
                    <i class="bi bi-gear"></i>
                </a>
            }
            @if (UserService.IsAdmin)
            {
                <a href="/admin" class="btn btn-sm btn-outline-light">
                    <i class="bi bi-shield-lock"></i> @L["Nav_Admin"]
                </a>
            }
        </div>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> @L["Nav_Home"]
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="explore">
                <span class="bi bi-compass" aria-hidden="true"></span> @L["Nav_Explore"]
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="search">
                <span class="bi bi-search" aria-hidden="true"></span> @L["Nav_Search"]
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="orgs">
                <span class="bi bi-people" aria-hidden="true"></span> @L["Nav_Organizations"]
            </NavLink>
        </div>

        @if (UserService.IsLoggedIn)
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="notifications">
                    <span class="bi bi-bell" aria-hidden="true"></span> @L["Nav_Notifications"]
                    @if (UnreadCount > 0)
                    {
                        <span class="badge bg-danger rounded-pill ms-2">@UnreadCount</span>
                    }
                </NavLink>
            </div>

            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"/profile/{UserService.Username}")">
                    <span class="bi bi-person-circle" aria-hidden="true"></span> @L["Nav_Profile"]
                </NavLink>
            </div>

            <div class="nav-item px-3">
                <NavLink class="nav-link" href="settings">
                    <span class="bi bi-gear" aria-hidden="true"></span> @L["Nav_Settings"]
                </NavLink>
            </div>
        }

        @if (UserService.IsAdmin)
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="admin">
                    <span class="bi bi-shield-lock" aria-hidden="true"></span> @L["Nav_Admin"]
                </NavLink>
            </div>
        }
    </nav>
</div>

@code {
    private int UnreadCount { get; set; }

    protected override async Task OnInitializedAsync()
    {
        if (UserService.IsLoggedIn)
        {
            UnreadCount = await NotificationService.GetUnreadCountAsync(UserService.Username);
        }
    }
}
